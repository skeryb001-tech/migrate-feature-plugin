interface:
  display_name: "跨平台功能迁移 SOP"
  short_description: "Web、混合客户端与原生客户端的行为/UI 等价迁移规范"
  default_prompt: "使用 $migrate-feature 迁移指定 Web 或客户端功能，保持行为与 UI 等价，并按目标平台结构落盘和验收。"

policy:
  allow_implicit_invocation: true
