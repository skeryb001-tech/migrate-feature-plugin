interface:
  display_name: "前端功能迁移 SOP"
  short_description: "行为等价、目标优先的跨项目与跨页面前端完整功能迁移规范"
  default_prompt: "使用 $migrate-feature 迁移指定前端功能，保持行为与 UI 等价，并按目标项目结构落盘和验收。"

policy:
  allow_implicit_invocation: true
